// The pages linked from the index. This is a frontend concern only: the server
// finds each page's shell by walking the built dist/ tree, so adding a site
// here (and a frontend/<site>/index.html) needs no backend change.
export const sites = [
  "/dates/",
  "/timetracker/",
  "/va/",
  "/food-days/",
  "/hash-data/",
  "/map/",
];
